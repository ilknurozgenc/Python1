# -*- coding: utf-8 -*-
"""gogleplaystore_dataset.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1ZXbIW7IaVnesKogSBJilrYfQlEexOfzW
"""

import pandas as pd
import numpy as np

df=pd.read_csv('Google-Playstore.csv')

df.head()

df.columns  #stun isimleri

df.shape   #satır, stün sayısını görmek

df.dtypes   #verisetindeki başlık türleri

"""EKSİK VERİ"""

df.isnull().sum()

import seaborn as sns

sns.set_theme()
sns.set(rc={"figure.dpi": 380, "figure.figsize":(12,9)})

sns.heatmap(df.isnull(),cbar= False)   #eksik verileri gösteren grafik görseli

rating_median=df["Rating"].median()
rating_median

df.dropna(inplace=True)

df.isnull().sum().sum()

df.info

print(len(df["Size"].unique()))  #size stunundaki değerler
df["Size"].unique()

df.Size.head()

df.Size.describe()  #verilerin tiplerinden kaç tane olduğunu

df["Installs"].unique()  #değerleri buluyor, kaç değer old ekrana yadırıyo

df.Installs=df.Installs.apply(lambda x:x.replace("+"," "))
df.Installs=df.Installs.apply(lambda x:int(x))

df["Installs"].head(10)

df["Installs"].value_counts()

df["Installs"].replace("0+" , "50+", inplace=True)  # 0+ 50+ yaptık
df["Installs"].head(20)

df.head()

df.dtypes

df["Size"].value_counts().plot(kind="bar", color="black")

import matplotlib.pyplot as plt
sns.countplot(y="Rating Count", data= df)
plt.title("contcent rating with their counts")

cat_num=df["Category"].value_counts()
sns.barplot(x=cat_num, y=cat_num.index, data=df)
plt.title("the number of category",size=20)

sns.scatterplot(data=df, y="Category", x="Price")  #en yüksek para verilen uygulamalar verilerinin grafikleri

sns.heatmap(df.corr(), annot=True, linewidths=5, fmt=".2f")  #ısı grafiğine göre grafik oluşturma

sns.histplot(df["Rating"], kde= True)    #rating dağılımını bulma


